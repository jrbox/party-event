services: # Here we define our service(s)
    web-app-service: # The name of the service
      container_name: party-event-web-app  # Container name
      build: ./web-app # Location of our Dockerfile
      ports: 
        - '80:80' # Port mapping
